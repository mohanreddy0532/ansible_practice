- name: Install Nginx
  ansible.builtin.yum:
    name: nginx
    state: present

- name: Download Frontend code and unarchive
  ansible.builtin.unarchive:
    src: https://github.com/roboshop-devops-project/frontend/archive/main.zip
    dest: /tmp
    remote_src: yes
